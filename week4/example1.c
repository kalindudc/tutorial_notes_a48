#include<stdio.h>
#include<stdlib.h>
#include<string.h>

// make a struct to store music record information

void create_record(/* take in a pointer to the struct */) {

    // ask for user input to fill in the record
    
}

void print_record(/* take in a pointer to the struct */) {

    // print our all the information of the record
}

int main() {

    // create an array of records

    // populate each record using create record

    // print out each record

}