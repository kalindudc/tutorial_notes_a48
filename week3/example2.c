#include<stdio.h>
#include<string.h>

void count_repeats(char * str, int * count) {
    // count the number of occurences of characters in str
    
}

int main() {

    // create a string
    char str[100] = "Hello, This is a very cool looking string.\0";

    // creat the count array
    // ??

    return 0;
}